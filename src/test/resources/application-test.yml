---

spring:
  datasource:
    username: "sa"
    password:
    driverClassName: "org.h2.Driver"

logging:
  level:
    root: DEBUG


---

spring:
  config:
    activate:
      on-profile: import_clock_in-feature
  datasource:
    url: "jdbc:h2:mem:import_clock_in-feature"
